# Project-specific R8 rules belong here. Flutter and the Android plugins used
# by MSS already publish their required consumer rules, so broad keep rules
# would only make the release larger. Add narrow rules only when an optimized
# release test demonstrates that reflection or JNI metadata is being removed.
